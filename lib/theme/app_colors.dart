import 'package:flutter/material.dart';

class AppColors {
  // Cores principais do AgroTrace
  static const Color primary = Color(0xFF2E7D32); // Verde escuro profissional
  static const Color primaryLight = Color(0xFF4CAF50); // Verde médio
  static const Color primaryDark = Color(0xFF1B5E20); // Verde escuro

  static const Color secondary = Color(0xFF8BC34A); // Verde claro
  static const Color secondaryLight = Color(0xFFAED581); // Verde claro suave
  static const Color secondaryDark = Color(0xFF689F38); // Verde escuro suave

  // Cores neutras modernas
  static const Color background = Color(0xFFFAFAFA); // Cinza muito claro
  static const Color surface = Color(0xFFFFFFFF); // Branco puro
  static const Color surfaceVariant = Color(0xFFF5F5F5); // Cinza claro

  // Cores de texto
  static const Color textPrimary = Color(0xFF212121); // Preto suave
  static const Color textSecondary = Color(0xFF757575); // Cinza médio
  static const Color textLight = Color(0xFFBDBDBD); // Cinza claro
  static const Color textOnPrimary = Color(0xFFFFFFFF); // Branco

  // Cores de status
  static const Color success = Color(0xFF4CAF50); // Verde
  static const Color successLight = Color(0xFFE8F5E8); // Verde claro
  static const Color warning = Color(0xFFFF9800); // Laranja
  static const Color warningLight = Color(0xFFFFF3E0); // Laranja claro
  static const Color error = Color(0xFFF44336); // Vermelho
  static const Color errorLight = Color(0xFFFFEBEE); // Vermelho claro
  static const Color info = Color(0xFF2196F3); // Azul
  static const Color infoLight = Color(0xFFE3F2FD); // Azul claro

  // Cores de destaque
  static const Color accent = Color(0xFFFF5722); // Laranja vibrante
  static const Color accentLight = Color(0xFFFFE0B2); // Laranja claro

  // Cores básicas
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color transparent = Color(0x00000000);

  // Cores de borda e divisão
  static const Color border = Color(0xFFE0E0E0); // Cinza claro
  static const Color divider = Color(0xFFE0E0E0); // Cinza claro
  static const Color shadow = Color(0x1A000000); // Preto com 10% opacidade

  // Cores específicas para animais
  static const Color maleColor = Color(0xFF2196F3); // Azul para machos
  static const Color femaleColor = Color(0xFFE91E63); // Rosa para fêmeas
  static const Color availableColor = Color(
    0xFF4CAF50,
  ); // Verde para disponível
  static const Color soldColor = Color(0xFF757575); // Cinza para vendido
  static const Color reservedColor = Color(
    0xFFFF9800,
  ); // Laranja para reservado
}
